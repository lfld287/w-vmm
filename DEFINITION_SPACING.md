# Rust 定义间空行规范

本文规定 Rust 代码中定义间空行的补充规则与验证流程。格式检查通过并不代表定义间距符合本规范，仍需单独全量复查。

## 修改规则

- 类型定义、`impl` 块、函数和方法之间，缺少空行的全部补一行；已有空行保持不变。
- `trait`、`extern` 块中相邻的函数声明同样隔一行。
- 空行加在所属定义的文档注释和属性之前，例如 `///`、`#[derive]`、`#[test]`。文档注释、属性及其所属定义保持紧贴，不在它们之间插入空行。
- 导入和常量保留连续分组，分组与后续定义之间隔一行。
- 仅补空行，不删除或修改原有代码、注释、属性，不调整顺序、缩进或其他格式，保持代码内容和行为不变。

## 验证流程

1. 全量复查项目内 Rust 代码的相邻定义，包括类型定义、`impl` 块、函数、方法，以及 `trait`、`extern` 中的函数声明，检查是否遗漏。
2. 检查每处新增空行的位置，确认它位于文档注释和属性之前，并确认导入和常量仍保留连续分组。
3. 在仓库根目录运行以下命令：

   ```sh
   cargo fmt --all -- --check
   git diff --check
   git diff --ignore-blank-lines --exit-code
   ```

4. 确认格式检查和差异空白检查通过，忽略空行后的差异为空且命令退出码为 `0`。同时查看普通 `git diff`，确认仅新增空行，没有删除已有空行。

`cargo fmt --all -- --check` 不能代替定义间距复查，即使当前格式检查已通过，也必须执行上述全量检查。

以上差异验证针对本次代码空行修改。若工作区已有其他改动，需先记录基线，单独比较本次修改前后的代码，避免将已有差异误判为本次变更。普通 `git diff` 不包含未跟踪文件；暂存区的修改需另用 `git diff --cached` 配合相同检查选项复查。本文档的新增应与代码空行修改分别验收。
